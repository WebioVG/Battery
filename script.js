// Declare variables
let notesKey = ['keyQ', 'keyS', 'keyD', 'keyF', 'keyG', 'keyH', 'keyJ', 'keyK', 'keyL'];

// Get HTML elements
let notes = document.getElementsByClassName('select');

// Main
window.addEventListener('keydown', function(event) {
    console.log(event.code);

    if (event.code = 'keyQ') {
        
    }
})